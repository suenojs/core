{
  "name": "@sueno/logger",
  "version": "0.1.0",
  "description": "Flexible and type-safe logging library for Sueno",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "benchmark": "bun run --smol benchmark/simple.bench.ts"
  },
  "dependencies": {
    "chalk": "^4.1.2",
    "fast-redact": "^3.5.0",
    "pino": "^8.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "bun-types": "latest",
    "mitata": "^0.1.6",
    "winston": "^3.12.0",
    "pino": "^8.19.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
